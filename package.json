{
  "name": "back-end-finance",
  "version": "0.0.1",
  "type": "module",
  "description": "Projeto de banco de dados feito com PostgreSQL para armazenar valores do front End",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "npx prisma generate && npx prisma migrate deploy",
    "vercel-build": "npx prisma generate && npx prisma migrate deploy"
  },
  "author": "Wesley",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "prisma-extension-pagination": "^0.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prisma": "^5.7.1"
  }
}
